apiVersion: v1
kind: Service
metadata:
  name: admin-service-nodeport
  namespace: bucktuality

spec:
  type: NodePort

  selector:
    app: admin-service

  ports:
    - name: http
      port: 5013
      targetPort: 5013
      nodePort: 31013