apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: bucktuality
  labels:
    app: notification-service

spec:
  replicas: 1

  selector:
    matchLabels:
      app: notification-service

  template:
    metadata:
      labels:
        app: notification-service

    spec:
      imagePullSecrets:
        - name: dockerhub-secret

      containers:
        - name: notification-service
          image: jpsinh27/bucktuality-notification-service:latest
          imagePullPolicy: Always

          ports:
            - name: http
              containerPort: 8085

          env:
            - name: KAFKA_BROKER
              value: "kafka:9092"

            - name: SQL_CONNECTION_STRING
              valueFrom:
                secretKeyRef:
                  name: notification-service-secret
                  key: sql-connection-string

          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"

            limits:
              cpu: "300m"
              memory: "256Mi"

          startupProbe:
            httpGet:
              path: /health
              port: 8085
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 24

          readinessProbe:
            httpGet:
              path: /health
              port: 8085
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3

          livenessProbe:
            httpGet:
              path: /health
              port: 8085
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3

---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: bucktuality
  labels:
    app: notification-service

spec:
  type: ClusterIP

  selector:
    app: notification-service

  ports:
    - name: http
      port: 8085
      targetPort: 8085