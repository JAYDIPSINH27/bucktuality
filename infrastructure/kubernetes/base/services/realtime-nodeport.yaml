apiVersion: v1
kind: Service
metadata:
  name: realtime-service-nodeport
  namespace: bucktuality

spec:
  type: NodePort

  selector:
    app: realtime-service

  ports:
    - name: http
      port: 5005
      targetPort: 5005
      nodePort: 31005