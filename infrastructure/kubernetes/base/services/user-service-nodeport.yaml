apiVersion: v1
kind: Service
metadata:
  name: user-service-nodeport
  namespace: bucktuality
spec:
  type: NodePort
  selector:
    app: user-service
  ports:
    - name: http
      port: 5010
      targetPort: 5010
      nodePort: 31010