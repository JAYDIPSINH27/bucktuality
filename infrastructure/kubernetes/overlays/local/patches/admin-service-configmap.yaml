apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-service
  namespace: bucktuality
spec:
  template:
    spec:
      containers:
        - name: admin-service
          envFrom:
            - configMapRef:
                name: admin-service-config
