apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: bucktuality
spec:
  template:
    spec:
      containers:
        - name: analytics-service
          envFrom:
            - configMapRef:
                name: analytics-service-config
