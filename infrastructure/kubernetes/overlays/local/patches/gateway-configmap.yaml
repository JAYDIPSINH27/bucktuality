apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  namespace: bucktuality
spec:
  template:
    spec:
      containers:
        - name: gateway
          envFrom:
            - configMapRef:
                name: gateway-config
