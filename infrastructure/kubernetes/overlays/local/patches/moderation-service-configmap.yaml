apiVersion: apps/v1
kind: Deployment
metadata:
  name: moderation-service
  namespace: bucktuality
spec:
  template:
    spec:
      containers:
        - name: moderation-service
          envFrom:
            - configMapRef:
                name: moderation-service-config
