apiVersion: apps/v1
kind: Deployment
metadata:
  name: presence-service
  namespace: bucktuality
spec:
  template:
    spec:
      containers:
        - name: presence-service
          envFrom:
            - configMapRef:
                name: presence-service-config
