apiVersion: apps/v1
kind: Deployment
metadata:
  name: realtime-service
  namespace: bucktuality
spec:
  template:
    spec:
      containers:
        - name: realtime-service
          envFrom:
            - configMapRef:
                name: realtime-service-config
